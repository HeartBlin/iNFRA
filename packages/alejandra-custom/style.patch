diff --git a/src/alejandra/src/rules/attr_set.rs b/src/alejandra/src/rules/attr_set.rs
index cb9a106..f8e94b8 100644
--- a/src/alejandra/src/rules/attr_set.rs
+++ b/src/alejandra/src/rules/attr_set.rs
@@ -58,6 +58,8 @@ pub(crate) fn rule(
     steps.push_back(crate::builder::Step::Format(child));
     if vertical {
         steps.push_back(crate::builder::Step::Indent);
+    } else {
+        steps.push_back(crate::builder::Step::Whitespace);
     }

     let mut item_index: usize = 0;
@@ -116,8 +118,10 @@ pub(crate) fn rule(
         steps.push_back(crate::builder::Step::Dedent);
         steps.push_back(crate::builder::Step::NewLine);
         steps.push_back(crate::builder::Step::Pad);
+    } else if items_count > 0 {
+        steps.push_back(crate::builder::Step::Whitespace);
     }
-    steps.push_back(crate::builder::Step::Format(child));

+    steps.push_back(crate::builder::Step::Format(child));
     steps
 }
diff --git a/src/alejandra/src/rules/lambda.rs b/src/alejandra/src/rules/lambda.rs
index 371029e..a9f341c 100644
--- a/src/alejandra/src/rules/lambda.rs
+++ b/src/alejandra/src/rules/lambda.rs
@@ -39,6 +39,13 @@ pub(crate) fn rule(
     // :
     let child = children.get_next().unwrap();
     steps.push_back(crate::builder::Step::Format(child));
+    let next = children.peek_next().unwrap();
+    if vertical
+        && next.kind() != rnix::SyntaxKind::NODE_LAMBDA
+        && build_ctx.indentation == 0
+    {
+        steps.push_back(crate::builder::Step::NewLine);
+    }

     // /**/
     let mut comment = false;
@@ -56,6 +63,7 @@ pub(crate) fn rule(
     let child = children.get_next().unwrap();
     if vertical {
         if comment
+            || build_ctx.indentation == 0
             || !matches!(
                 child.kind(),
                 rnix::SyntaxKind::NODE_ATTR_SET
@@ -84,6 +92,7 @@ pub(crate) fn rule(
             steps.push_back(crate::builder::Step::NewLine);
             steps.push_back(crate::builder::Step::Pad);
             steps.push_back(crate::builder::Step::FormatWider(child));
+
             if should_indent {
                 steps.push_back(crate::builder::Step::Dedent);
             }
diff --git a/src/alejandra/src/rules/list.rs b/src/alejandra/src/rules/list.rs
index 4fbb241..f94e225 100644
--- a/src/alejandra/src/rules/list.rs
+++ b/src/alejandra/src/rules/list.rs
@@ -23,6 +23,8 @@ pub(crate) fn rule(
     steps.push_back(crate::builder::Step::Format(child));
     if vertical {
         steps.push_back(crate::builder::Step::Indent);
+    } else {
+        steps.push_back(crate::builder::Step::Whitespace);
     }

     let mut item_index: usize = 0;
@@ -90,8 +92,10 @@ pub(crate) fn rule(
         steps.push_back(crate::builder::Step::Dedent);
         steps.push_back(crate::builder::Step::NewLine);
         steps.push_back(crate::builder::Step::Pad);
+    } else if items_count > 0 {
+        steps.push_back(crate::builder::Step::Whitespace);
     }
-    steps.push_back(crate::builder::Step::Format(child));

+    steps.push_back(crate::builder::Step::Format(child));
     steps
 }
diff --git a/src/alejandra/src/rules/pattern.rs b/src/alejandra/src/rules/pattern.rs
index c2f7662..d1d1140 100644
--- a/src/alejandra/src/rules/pattern.rs
+++ b/src/alejandra/src/rules/pattern.rs
@@ -16,18 +16,17 @@ pub(crate) fn rule(
         || !pattern.comments_after_initial_at.is_empty()
         || !pattern.comments_before_end_at.is_empty();

-    let has_ellipsis = pattern.arguments.iter().any(|argument| {
-        if argument.item.is_some() {
-            argument.item.as_ref().unwrap().kind()
-                == rnix::SyntaxKind::TOKEN_ELLIPSIS
-        } else {
-            false
-        }
-    });
+    // let has_ellipsis = pattern.arguments.iter().any(|argument| {
+    //     if argument.item.is_some() {
+    //         argument.item.as_ref().unwrap().kind()
+    //             == rnix::SyntaxKind::TOKEN_ELLIPSIS
+    //     } else {
+    //         false
+    //     }
+    // });

     let arguments_count = pattern.arguments.len();
-
-    let arguments_count_for_tall = if has_ellipsis { 2 } else { 1 };
+    let arguments_count_for_tall = 100;

     let vertical = has_comments
         || arguments_count > arguments_count_for_tall
@@ -64,6 +63,8 @@ pub(crate) fn rule(
     ));
     if vertical {
         steps.push_back(crate::builder::Step::Indent);
+    } else {
+        steps.push_back(crate::builder::Step::Whitespace);
     }

     // arguments
@@ -136,7 +137,10 @@ pub(crate) fn rule(
             steps.push_back(crate::builder::Step::NewLine);
             steps.push_back(crate::builder::Step::Pad);
         }
+    } else if arguments_count > 0 {
+        steps.push_back(crate::builder::Step::Whitespace);
     }
+
     steps.push_back(crate::builder::Step::Token(
         rnix::SyntaxKind::TOKEN_R_BRACE,
         "}".to_string(),
